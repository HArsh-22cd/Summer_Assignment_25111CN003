#include<iostream>
using namespace std;
int main() {
    int N, sum=0, factors, i;
    cout << "N is the number you want factor of" << endl;
    cout << "Enter N: ";
    cin >> N;
    cout << "Factors of the number are : ";
    for(int i=1; i<=N; i++) {
        factors = N%i;
        if(factors == 0)
        cout << i << ", ";
    }
    return 0;
}
